namespace AutofacResolver.Modules
{
  using Autofac;

  public class AbstractConfigurationManager : Module
  {
    protected override void Load(ContainerBuilder builder)
    {
      base.Load(builder);
    }
  }
}
